---
trigger: always_on
---

# Workflow: Test React Component

Trigger this to generate Jest/React Testing Library tests for a selected component.

**Requirements:**

- Cover Happy Path, Edge Cases, and Error Handling.
- Mock external dependencies (NextAuth, Pusher, API calls).
- Use `@testing-library/user-event` for interactions.
- Update the **Task List** before generating the code.
